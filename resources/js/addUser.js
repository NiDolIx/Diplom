const btnUserAdd = document.getElementById('add-dispatcher-btn');

btnUserAdd.addEventListener('click', function() {
    const addUser = document.getElementById('add-dispatcher').classList.toggle('hidden');
})